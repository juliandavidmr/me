---
import '@/styles/global.css';

// eslint-disable-next-line import/no-unresolved
import { ClientRouter } from 'astro:transitions';

import { AppConfig } from '@/utils/AppConfig';

export interface Props {
  head: {
    title: string;
    description: string;
  };
}

const {
  head: { title, description },
} = Astro.props as Props;
---

<html lang="en" class="scroll-smooth bg-[#f7f5f0] dark:bg-[#0b0b0b]">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content={AppConfig.author} />

    <link rel="alternate" type="application/rss+xml" href="/rss.xml" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <ClientRouter />
  </head>
  <body
    class="m-0 bg-[#f7f5f0] font-['Geist','Geist_Sans',ui-sans-serif,system-ui,sans-serif] text-sm leading-[1.55] text-[#0d0d0d] antialiased dark:bg-[#0b0b0b] dark:text-[#f4f1ea]"
  >
    <slot />
  </body>
</html>
